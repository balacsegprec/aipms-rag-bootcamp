# AI-PMS RAG Bootcamp — No-GPU Setup
# Core Dependencies for Production Environment

# ============================================================================
# CRITICAL: This file MUST NOT contain hardcoded API keys
# Store all API keys in .env file (see .env.example)
# ============================================================================

# ============================================================================
# LLM Inference — Cloud APIs (No GPU Required)
# ============================================================================
openai>=1.3.0              # OpenAI client library (used for Groq, OpenRouter, Cerebras)
google-genai>=0.3.0        # Google Gemini API client

# ============================================================================
# Embedding Models — Local CPU
# ============================================================================
sentence-transformers>=2.6.0  # Bi-encoder embeddings (all-MiniLM, BGE, Nomic)
torch>=2.0.0                  # PyTorch (CPU mode, no CUDA)
transformers>=4.35.0          # Hugging Face transformers

# ============================================================================
# Vector Database & Search
# ============================================================================
psycopg2-binary>=2.9.0     # PostgreSQL client (for pgvector + pg_trgm)
pgvector>=0.2.0            # pgvector Python client (if needed)
chromadb>=0.4.0            # ChromaDB in-memory vector store (fallback)
faiss-cpu>=1.7.0           # FAISS for benchmarking (CPU version)

# ============================================================================
# Orchestration Frameworks
# ============================================================================
langchain>=0.1.0           # LangChain core library
langgraph>=0.0.20          # LangGraphs for agentic workflows
llama-index>=0.9.0         # LlamaIndex for multi-hop retrieval

# ============================================================================
# Evaluation Framework
# ============================================================================
ragas>=0.0.20              # RAG evaluation framework (Faithfulness, etc.)
scikit-learn>=1.3.0        # ML utilities for RAGAS metrics

# ============================================================================
# Experiment Tracking
# ============================================================================
mlflow>=2.10.0             # Experiment tracking + model registry

# ============================================================================
# Data Processing & Utilities
# ============================================================================
pandas>=2.0.0              # Data manipulation
numpy>=1.24.0              # Numerical computing
pdfplumber>=0.10.0         # PDF text extraction (for GCC contracts)
python-dotenv>=1.0.0       # Load .env files safely

# ============================================================================
# Visualization & Logging
# ============================================================================
umap-learn>=0.5.0          # UMAP for embedding visualization
matplotlib>=3.7.0          # Plotting
seaborn>=0.12.0            # Statistical visualization
python-json-logger>=2.0.0  # Structured JSON logging

# ============================================================================
# Utilities & Helpers
# ============================================================================
requests>=2.31.0           # HTTP client (for HuggingFace Inference API)
tqdm>=4.66.0               # Progress bars
pydantic>=2.0.0            # Data validation + settings management
typing-extensions>=4.7.0   # Type hints backport

# ============================================================================
# Development & Testing (Optional, uncomment if needed)
# ============================================================================
# pytest>=7.4.0              # Unit testing
# pytest-cov>=4.1.0          # Coverage reporting
# black>=23.9.0              # Code formatting
# flake8>=6.1.0              # Linting
# mypy>=1.5.0                # Type checking
